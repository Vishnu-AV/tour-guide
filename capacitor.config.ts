import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tourguide.ionic',
  appName: 'myApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
